[project]
name = "opportunity-router"
version = "0.1.0"
description = "Policy-authorized opportunity router Phase 1 pure library"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opportunity_router"]
